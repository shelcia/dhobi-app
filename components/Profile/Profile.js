import React from "react";
import { Text } from "react-native";

const Profile = () => {
  return (
    <React.Fragment>
      <Text>Profile</Text>
    </React.Fragment>
  );
};

export default Profile;
